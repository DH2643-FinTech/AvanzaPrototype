/**
 * DEPRECATED: Mongoose is not used in this project yet!
 */

// import mongoose, {Document, Schema} from 'mongoose';

// export interface CompanyID extends Document {
//     _id: string;
//     name: string;
// }

// const companySchema = new Schema<CompanyID>({
//     name: {type: String, required: true},
//     _id: {type: String, required: true}
// });

// const CompanyModel = mongoose.models.Company || mongoose.model<CompanyID>('Company', companySchema);
// export default CompanyModel;